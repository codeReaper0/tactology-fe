/* eslint-disable @typescript-eslint/no-unused-vars */
type InputProps = {
  label: string;
  name: string;
  placeholder: string;
  type: string;
};

type PasswordInput = {
  label: string;
  name: string;
  placeholder: string;
};
